# Smart PomoTodo 生产环境部署指南（Windows 手动部署）

## 📋 部署概览

本文档指导你在 Windows 本地服务器上手动部署 Smart PomoTodo 生产环境。

**部署架构：**
- MySQL 8.0（端口 3306）
- Redis（端口 6379）
- Spring Boot 后端（端口 8080）
- Vue 前端（端口 3000）

---

## ✅ 前置条件

1. ✅ Java JDK 17+
2. ✅ Maven 3.8+
3. ✅ Node.js 16+
4. ✅ MySQL 8.0（已安装）
5. ✅ Redis（已安装）

---

## 🚀 快速开始

### 第一步：创建数据库（已完成）

请确认已在 MySQL 中执行：

```sql
-- 创建数据库
CREATE DATABASE pomotodo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'pomotodo'@'localhost' IDENTIFIED BY '040317';
GRANT ALL PRIVILEGES ON pomotodo.* TO 'pomotodo'@'localhost';
FLUSH PRIVILEGES;
```

### 第二步：检查服务状态

确保 MySQL 和 Redis 服务正在运行：

```powershell
# 检查 MySQL
sc query MySQL80

# 检查 Redis
redis-cli ping
# 应该返回: PONG
```

### 第三步：启动生产环境

在项目根目录执行：

```cmd
start-production.bat
```

脚本会：
1. 加载环境变量
2. 检查 MySQL 和 Redis 服务
3. 验证数据库连接
4. 启动后端服务（生产模式）

### 第四步：启动前端

在另一个命令行窗口执行：

```cmd
cd frontend
npm run dev
```

### 第五步：访问应用

- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8080
- **API 文档**: http://localhost:8080/swagger-ui.html （如果配置了 Swagger）

---

## 🔧 详细配置说明

### 环境变量配置

所有配置都在 `backend\.env` 文件中：

```bash
# 数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=pomotodo
MYSQL_USER=pomotodo
MYSQL_PASSWORD=040317

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT 密钥（已自动生成，不要修改）
JWT_SECRET=P67p1P75qbBCdVh8wJoK2ZaIIVbhIdlUFUq6tqY4LzCHZCxFeF6nAVyVm7WCYlnXBKD/NhZHO7gR2h4wPUkNyg==
```

### 首次启动说明

**第一次启动时：**
- Hibernate 会自动创建数据库表结构
- 这可能需要 30-60 秒，请耐心等待
- 控制台会显示表创建日志

**启动成功后：**
- MySQL 中会自动创建以下表：
  - `users` - 用户表
  - `tasks` - 任务表
  - `teams` - 团队表
  - `team_members` - 团队成员表
  - `focus_logs` - 专注记录表
  - `push_subscriptions` - 推送订阅表

---

## 📊 常用操作

### 数据库备份

双击运行 `backup-database.bat`：
- 备份文件保存在 `C:\Backups\PomoTodo`
- 自动清理 30 天前的备份

### 查看日志

**后端日志：**
```cmd
cd backend\logs
type application.log
```

**实时查看日志：**
```cmd
cd backend\logs
tail -f application.log
```

### 停止服务

- **后端**: 在运行 `start-production.bat` 的窗口中按 `Ctrl+C`
- **前端**: 在运行 `npm run dev` 的窗口中按 `Ctrl+C`
- **MySQL**: `net stop MySQL80`
- **Redis**: 根据安装方式不同，使用对应命令

---

## 🔍 故障排查

### 问题1：无法连接到数据库

**症状：**
```
Connection refused: connect
```

**解决方案：**
1. 检查 MySQL 服务是否运行：`sc query MySQL80`
2. 检查用户是否存在：
   ```sql
   mysql -u root -p
   SELECT user, host FROM mysql.user WHERE user='pomotodo';
   ```
3. 检查密码是否正确

### 问题2：端口被占用

**症状：**
```
Port 8080 was already in use
```

**解决方案：**
```cmd
# 查找占用端口的进程
netstat -ano | findstr :8080

# 结束进程（将 <PID> 替换为实际的进程ID）
taskkill /PID <PID> /F
```

### 问题3：Redis 连接失败

**症状：**
```
Connection refused: no further information: localhost/127.0.0.1:6379
```

**解决方案：**
1. 检查 Redis 服务状态
2. 如果使用 Memurai，检查服务是否启动
3. 检查 Redis 端口配置

### 问题4：表创建失败

**症状：**
```
Table 'pomotodo.users' doesn't exist
```

**解决方案：**
1. 检查数据库字符集是否为 utf8mb4
2. 检查用户是否有创建表权限
3. 检查 application-prod.yml 中的 ddl-auto 是否为 update

---

## 🔐 安全建议

### 1. 修改默认密码（生产环境强烈推荐）

当前密码 `040317` 是测试用的简单密码，生产环境请使用强密码：

```sql
-- 修改数据库用户密码
ALTER USER 'pomotodo'@'localhost' IDENTIFIED BY 'YourStrongPassword123!';
FLUSH PRIVILEGES;
```

然后更新 `backend\.env` 文件中的 `MYSQL_PASSWORD`。

### 2. 重新生成 JWT Secret

```powershell
# 生成新的密钥
openssl rand -base64 64

# 或者使用 PowerShell
[Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 } | ForEach-Object { [byte]$_ }))
```

### 3. 配置防火墙

如果需要从其他机器访问：
- 开放端口：8080（后端）、3000（前端）
- **不要**开放 3306 和 6379 到公网

### 4. 定期备份

建议设置 Windows 计划任务，每天自动运行 `backup-database.bat`。

---

## 📝 配置文件说明

### application-prod.yml

- **位置**: `backend\src\main\resources\application-prod.yml`
- **作用**: 生产环境配置
- **关键配置**:
  - `ddl-auto: update` - 自动更新表结构（首次部署）
  - 生产环境稳定后建议改为 `validate`

### .env

- **位置**: `backend\.env`
- **作用**: 环境变量配置
- **注意**: 已添加到 `.gitignore`，不会提交到 Git

---

## 🎯 性能优化建议

### 1. MySQL 优化

```sql
-- 查看慢查询日志是否开启
SHOW VARIABLES LIKE 'slow_query_log';

-- 开启慢查询日志（如果需要）
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 2;
```

### 2. Redis 优化

当前配置已启用连接池，如需调整：

```yaml
# application-prod.yml
spring:
  data:
    redis:
      lettuce:
        pool:
          max-active: 16  # 增加连接数
          max-idle: 8
```

### 3. JVM 优化

编辑 `start-production.bat`，在启动命令前添加：

```bat
set JAVA_OPTS=-Xms512m -Xmx2g -XX:+UseG1GC
```

---

## 📞 技术支持

遇到问题？

1. 查看后端日志：`backend\logs\application.log`
2. 检查数据库连接：使用 MySQL Workbench 或命令行
3. 检查 Redis：使用 `redis-cli` 命令

---

## ✅ 部署检查清单

- [ ] MySQL 服务已启动
- [ ] Redis 服务已启动
- [ ] 数据库 `pomotodo` 已创建
- [ ] 用户 `pomotodo` 已创建并有权限
- [ ] `.env` 文件配置正确
- [ ] 后端启动成功，无错误日志
- [ ] 前端启动成功
- [ ] 可以正常访问 http://localhost:3000
- [ ] 可以正常注册/登录
- [ ] 数据库表已自动创建

---

**祝你部署顺利！🚀**
